gitlab_runner_coordinator_url: "http:/35.228.88.166/"
gitlab_runner_registration_token: 'U4ozZUKPaeKhdYEaDpEo'
gitlab_runner_runners:
  - name: 'Docker GitLab Runner'
    executor: docker
    docker_image: 'alpine:latest'
    tags:
      - linux
      - docker
      - ubuntu
      - xenial
    docker_volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "/cache"
